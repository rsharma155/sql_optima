# Replication threshold rules removed (DEFECT-7):
# ha_replication_lag and ha_replication_lag_critical both fire on secondary_lag_seconds,
# which is already covered by the dynamic "replication_lag" rule in engine.go with
# hardware-tuned thresholds (default 60s, scales with observed p95 lag).
# Keeping both caused duplicate CurrentIssues entries with conflicting rule names.
#
# To add replication rules here in future, use ONLY binary/configuration checks
# (e.g. synchronization_health != 'HEALTHY') — not threshold comparisons on lag seconds.
