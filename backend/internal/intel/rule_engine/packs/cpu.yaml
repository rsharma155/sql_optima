# CPU threshold rules are evaluated dynamically in analysis/engine.go using
# core count, virtualization, and historical CPU/runnable baselines.
#
# Removed fixed rules:
#   - cpu_saturation (was avg_cpu_load > 85% AND runnable > 10)
#   - scheduler_starvation (was runnable > 50)
#   - cpu_burst (removed earlier — duplicated cpu_saturation)
