# Disk and I/O threshold rules are evaluated dynamically in analysis/engine.go
# using free-space percentiles, growth rate stddev, and p95 I/O latency.
#
# Removed fixed rules:
#   - low_disk_space (was free_disk_mb < 10240)
#   - rapid_disk_growth (was delta_data_mb > 500)
#   - io_latency_high (was read_latency_ms > 20)
