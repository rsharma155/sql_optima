# Memory threshold rules are evaluated dynamically in analysis/engine.go
# (evaluateDynamicRules) using RAM-scaled PLE floors, grant pending baselines,
# and history-tuned buffer cache hit ratio thresholds.
#
# Removed fixed rules (DEFECT-7 / YAML dedup):
#   - ple_collapse (was PLE < 300 seconds)
#   - memory_grant_pressure (was grants > 0)
#   - buffer_cache_hit_ratio_low (was BCHR < 90%)
