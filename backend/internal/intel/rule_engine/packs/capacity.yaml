# Capacity threshold rules are evaluated dynamically in analysis/engine.go.
#
# Removed fixed rules:
#   - storage_exhaustion, database_growth_acceleration (disk growth duplicates)
#   - backup_failure_risk (failed_jobs_24h > 0 — now dynamic)
