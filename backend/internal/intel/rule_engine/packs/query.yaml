# Query workload threshold rules are evaluated dynamically in analysis/engine.go
# using blocking session p95, TempDB usage p95, and spill warning baselines.
#
# Removed fixed rules:
#   - blocking_chains (was blocking_sessions > 5)
#   - tempdb_pressure (was tempdb_used_pct > 80)
#   - query_spills (was sort_warnings_per_sec > 1)
